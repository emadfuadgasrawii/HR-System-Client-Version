# United Fund HR System - Client Deployment Guide

## 📁 الملفات المجهزة للعميل

تم إنشاء نسخة جاهزة من المشروع في المجلد:
`C:\Users\emadf\Desktop\HR-System-Client-Version`

## 🔐 معلومات تسجيل الدخول

**اسم المستخدم:** admin  
**كلمة المرور:** admin123

> ⚠️ **مهم:** يرجى إخبار العميل بتغيير كلمة المرور بعد أول تسجيل دخول

## 📦 الملفات المضمنة

### 1. **ملفات الواجهة الأمامية (Frontend)**
- `dist/` - الملفات المجمعة جاهزة للنشر
- `public/` - الملفات العامة (PWA manifest, service worker, icons)

### 2. **ملفات السيرفر (Backend)**
- `server.ts` - ملف السيرفر الرئيسي
- `setup-database.sql` - سكريبت إعداد قاعدة البيانات فارغة

### 3. **ملفات التكوين**
- `package.json` - تبعيات المشروع
- `.env` - متغيرات البيئة
- `tsconfig.json` - إعدادات TypeScript
- `.gitignore` - الملفات المحددة للتجاهل

### 4. **ملفات التوثيق**
- `README.md` - دليل المستخدم للعميل
- `DEPLOYMENT-GUIDE.md` - هذا الملف (دليل النشر)

## 🚀 خطوات النشر على Bluehost

### 1. رفع الملفات
1. سجل الدخول إلى حساب Bluehost
2. انتقل إلى **File Manager**
3. ارفع جميع الملفات من مجلد `dist` إلى `public_html`
4. ارفع `server.ts` إلى `public_html`

### 2. إعداد قاعدة البيانات
1. أنشئ قاعدة بيانات MySQL جديدة في Bluehost
2. قم بتعديل `setup-database.sql` ليتوافق مع MySQL بدلاً من SQLite
3. قم بتشغيل السكريبت على قاعدة البيانات

### 3. تحديث إعدادات السيرفر
في ملف `server.ts`، قم بتحديث:
- معلومات اتصال قاعدة البيانات
- منفذ السيرفر (Port)
- إعدادات JWT

### 4. تثبيت التبعيات وتشغيل السيرفر
```bash
npm install
npm start
```

## 📱 تثبيت التطبيق كـ PWA

التطبيق يدعم التثبيت كتطبيق على الأجهزة:

### على Chrome/Edge:
1. افتح الموقع في المتصفح
2. اضغط على أيقونة التثبيت في شريط العنوان
3. اضغط "تثبيت"

### على Safari (iPhone/iPad):
1. افتح الموقع في Safari
2. اضغط على زر المشاركة
3. اضغط "إضافة إلى الشاشة الرئيسية"

## ✅ الميزات الجديدة للعميل

- ✅ **PWA Support** - يمكن تثبيته كتطبيق
- ✅ **Offline Support** - يعمل بدون إنترنت
- ✅ **قاعدة بيانات فارغة** - جاهز للبيانات الجديدة
- ✅ **مستخدم admin جاهز** - admin/admin123
- ✅ **أيقونة التطبيق** - أيقونة HR احترافية
- ✅ **دعم متعدد اللغات** - العربية والإنجليزية
- ✅ **دليل شامل** - README مفصل للعميل

## 📝 ملاحظات مهمة

1. **قاعدة البيانات:** السكريبت الحالي يستخدم SQLite، يجب تعديله لـ MySQL للعمل على Bluehost
2. **الأمان:** يجب تغيير JWT_SECRET في ملف .env
3. **HTTPS:** يجب تفعيل HTTPS على Bluehost ليعمل PWA بشكل صحيح
4. **Service Worker:** يجب أن يكون الموقع على HTTPS ليعمل Service Worker

## 🎯 الخطوات التالية

1. ✅ نسخ الملفات إلى الفولدر الجديد
2. ✅ إضافة ملفات PWA (manifest, service worker)
3. ✅ إنشاء أيقونة التطبيق
4. ✅ إنشاء سكريبت قاعدة البيانات فارغة
5. ✅ إنشاء ملف README للعميل
6. ⏳ تعديل سكريبت قاعدة البيانات لـ MySQL
7. ⏳ إضافة زر تنزيل التطبيق في الواجهة (اختياري)

---
تم إنجاز النسخة الأساسية للعميل بنجاح! 🎉
